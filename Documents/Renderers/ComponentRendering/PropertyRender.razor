
@if (Model is not null && PropertyInfo is not null)
{
    
}


@code {
    [Parameter]
    public object? Model { get; set; }
    
    [Parameter]
    public PropertyInfo? PropertyInfo { get; set; }
}